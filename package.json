{
  "name": "personal-deborah",
  "version": "1.0.0",
  "description": "Deborah's personal website",
  "scripts": {
    "build:sass": "node-sass -r --output-style compressed sass/styles.scss -o css",
    "watch:sass": "npm run build:sass && npm run build:sass -- -w",
    "start": "parcel index.html"
  },
  "author": "Deborah Yscava",
  "license": "MIT License",
  "devDependencies": {
    "node-sass": "^8.0.0",
    "parcel": "^2.8.0"
  }
}
